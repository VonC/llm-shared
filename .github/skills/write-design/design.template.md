# Design vX.Y.Z -- {Topic}

Reference {requirement type}: [{type}.vX.Y.Z.{topic}.md]({type}.vX.Y.Z.{topic}.md)

---

## Context for vX.Y.Z {topic}

{Optional short context paragraph: what triggered this design, what changed since the previous version, or what requirement this document answers.}

## Scope for vX.Y.Z {topic}

The vX.Y.Z outcomes are:

1. {Outcome 1}
2. {Outcome 2}
3. {Outcome 3}

Everything else is either supporting design context for those outcomes or explicitly deferred.

### In scope for vX.Y.Z {topic}

- {Delivered capability}
- {Delivered capability}
- {Delivered capability}

### Deferred from vX.Y.Z {topic} to {next version} and beyond

- {Deferred item}
- {Deferred item}

---

## Confirmed Technical Facts for vX.Y.Z {topic}

These facts were confirmed by inspecting the current codebase before writing this design.

**{Fact 1}**: {What is already true in the codebase, and why it matters for the design.}

**{Fact 2}**: {What is already true in the codebase, and why it matters for the design.}

**{Fact 3}**: {What is already true in the codebase, and why it matters for the design.}

---

## Current Behavior for vX.Y.Z {topic}

{Describe the current flow only when that context is needed for the design.}

```txt
{Optional current call chain, request flow, state flow, or sequence}
```

## Target Behavior for vX.Y.Z {topic}

{Describe the intended flow, constraints, and state transitions at design level.}

```txt
{Optional target call chain, request flow, state flow, or sequence}
```

---

## Design Area 1 for vX.Y.Z {topic}

### {Sub-area title}

{Describe one major design area: actors, data shape, API contract, state model, validation rule, or trust boundary.}

### {Sub-area title 2}

{Document the important rule, failure mode, or boundary condition for this design area.}

## Design Area 2 for vX.Y.Z {topic}

### {Sub-area title 3}

{Describe the next major design area. Repeat as needed for each feature slice or architectural concern.}

---

## Acceptance Cases for vX.Y.Z {topic}

{Add this section when concrete examples, edge cases, or boundary cases are needed to make the design testable and unambiguous.}

| Scenario | Expected outcome | Reason |
| ---- | ---- | ---- |
| `{Example 1}` | `{Expected outcome}` | `{Reason}` |
| `{Example 2}` | `{Expected outcome}` | `{Reason}` |
