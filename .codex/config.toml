# Project-local Codex settings for llm-shared.
#
# Project-local execution exception.
approval_policy = "never"
sandbox_mode = "danger-full-access"

# This repository is intentionally offline-by-default for the Codex web
# search tool. Enable search per task when current external information is
# required.
tools.web_search = false
