---
description: Produce an activity report for IT managers from one or more git working trees over a date window, using commit messages and Markdown diffs, with user topic choices and a French report by default. Use when the user asks for an activity report.
---

1. Locate the shared instruction body `instructions/activity-report.md`: in this
   workspace root when the workspace is llm-shared itself, else under the
   sibling clone `../llm-shared`, else under a `llm-shared` submodule folder.
2. Read that file in full.
3. Follow it exactly, treating any text given after the slash command as its
   arguments.
