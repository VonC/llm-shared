---
description: Independently review one exact staged implementation step
---

Locate the `llm-shared` repository as a sibling clone `../llm-shared` or as the
current project's `llm-shared` submodule folder. Read and follow its canonical
`instructions/code-reviewer.md` file.
