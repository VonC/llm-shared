---
description: Review a type.vX.Y.Z.topic.md document, and add an "Open-ended questions" section at the end of the document, with all the open questions you have about the type (feature-request, issue, design or plan), based on your review of that document.
---

1. Locate the shared instruction body `instructions/review-ask-questions.md`: in this
   workspace root when the workspace is llm-shared itself, else under the
   sibling clone `../llm-shared`, else under a `llm-shared` submodule folder.
2. Read that file in full.
3. Follow it exactly, treating any text given after the slash command as its
   arguments.
