---
description: Install, repair, or verify sensitive pre-commit and commit-msg hooks in the current Git repository using llm-shared’s Python and shared plus local replacement rules.
---

1. Locate the shared instruction body
   `instructions/install-sensitive-git-hooks.md`: in this workspace root when
   the workspace is llm-shared itself, else under the sibling clone
   `../llm-shared`, else under a `llm-shared` submodule folder.
2. Read that file in full.
3. Follow it exactly, treating any text given after the slash command as its
   arguments.
