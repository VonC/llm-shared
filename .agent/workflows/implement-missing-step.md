---
description: Finish only the missing work for a plan step after implementation-check marks it incomplete, using the validation plan missing-work section and applying implement-step or split-large-file as appropriate.
---

1. Locate the shared instruction body `instructions/implement-missing-step.md`: in this
   workspace root when the workspace is llm-shared itself, else under the
   sibling clone `../llm-shared`, else under a `llm-shared` submodule folder.
2. Read that file in full.
3. Follow it exactly, treating any text given after the slash command as its
   arguments.
