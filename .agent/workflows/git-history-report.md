---
description: Build a standalone HTML commit-history dashboard for one or more git working trees, including aggregated history, filters, contributor views, and the keep-the-notes analysis workflow. Use when the user asks for a git history report or dashboard.
---

1. Locate the shared instruction body `instructions/git-history-report.md`: in this
   workspace root when the workspace is llm-shared itself, else under the
   sibling clone `../llm-shared`, else under a `llm-shared` submodule folder.
2. Read that file in full.
3. Follow it exactly, treating any text given after the slash command as its
   arguments.
