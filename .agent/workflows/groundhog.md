---
description: Drive the groundhog test loop until the project objective is reached: full test pass and coverage gate, using ghog commands and fixing failures between runs.
---

1. Locate the shared instruction body `instructions/groundhog.md`: in this
   workspace root when the workspace is llm-shared itself, else under the
   sibling clone `../llm-shared`, else under a `llm-shared` submodule folder.
2. Read that file in full.
3. Follow it exactly, treating any text given after the slash command as its
   arguments.
