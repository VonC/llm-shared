"""Unit test package for tools/prompt_workflow_skill.py (pw skill foundation).

Fix: Add the nested test package marker so the pw skill tests live in the
``test_<module>/test_<module>_tdd.py`` form chosen for v0.9.0 (Q01).
"""

# eof
