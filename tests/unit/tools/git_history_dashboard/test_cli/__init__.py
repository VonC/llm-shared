"""Package markers for the git-history dashboard CLI tests.

Step 2 (v0.8.0): holds test_cli_tdd.py, covering the multi-repo orchestration in
``tools/git_history_dashboard/cli.py`` -- target resolution, the combined run,
the browser-open suppress flag, the per-repo skip, and the run summary.
"""

# eof
