"""Git-history dashboard unit test subpackage for llm-shared.

Step 0 (v0.8.0): the new nested test subpackage for the git_history_dashboard
tool, adopting the per-test-folder convention (plan Q01). It holds the relocated
build test and the per-module test leaves added across the v0.8.0 steps.
"""

# eof
