"""Acceptance test package for prompt_workflow end-to-end runs.

Fix: Add the nested test package marker; this feature moves the acceptance test
into the ``test_<module>/test_<module>_tdd.py`` form chosen for v0.9.0 (Q08).
"""

# eof
